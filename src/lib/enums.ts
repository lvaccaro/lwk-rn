
export enum Network {
    Testnet = 'testnet',
    Regtest = 'regtest',
    Mainnet = 'mainnet',
    Signet = 'signet',
}

export enum Chain {
  external = 'external',
  internal = 'internal',
}

export enum ClientNames {
  Electrum = 'Electrum',
  Esplora = 'Esplora',
  Rpc = 'Rpc',
}